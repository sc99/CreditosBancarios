<?php
  $entityCustom=new EntityCustomer();

  $entityCustom->EntityCustomer();
  $entityCustom->EntityCustomer(request:Array);
  $entityCustom->addCredit(Credit:CreditModel): boolean;
  $entityCustom->renovateCredit(creditld:int): boolean;
  $entityCustom->cancelCredit(creditld:int):boolean;
  //siento que no es lo requerido. Necesito entender bien lo que espera amigo
 ?>
